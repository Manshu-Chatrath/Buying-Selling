<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buying and Selling</title>
    <link rel="stylesheet" href="/nav.css">
    <link rel="stylesheet" href="/items.css">
    <link rel="stylesheet" href="/search.css">
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="fontawesome-free-5.14.0-web/css/all.css">

</head>
<body>
    <%- include('./nav.ejs')  %>
<main class="main">
<%- include('./search.ejs') %>
<section class="items">
    <%if(!products || products.length===0) {%>
        <h1 style="text-align: center;">No Products Found!</h1>
        <% } else if(products) {%>
    <%  for(let i=0;i<products.length;i++) { %>
    <div class="item">
        <div class="common item-front">
        <div class="title"><%=products[i].title%></div>
        <img class="image" src="<%=products[i].image%>" alt="">
        <div class="price">Price: <%=products[i].price%>$</div>
    </div>
    <div class="common item-back "style="width: 100%;">
        <div class="title-back"><%=products[i].title%></div>
        <div class="summary"><%=products[i].summary%></div>
            <input type="hidden" name="productId" value="<%=products[i].id%>">
            <input type="hidden" name="sellerId" value="<%=products[i].seller_id%>">
        <div class="buttons">
            <form action="/details/<%=products[i].id%>" method="GET">
                <button class="view" style="width: 100%;">Details</button>
    </form>    
    </div>
    </div>
    </div>
    <% } %>
</section>
<% }%>
</main>
<script>
    let consumer=document.querySelectorAll('.view');
    console.log(consumer);
    console.log("hohoh")
    for(let i=0;i<consumer.length;i++)
    {
    consumer[i].addEventListener('click',function(e)
    {console.log("boom")
     location.href="details"
    })
}
    let recently=document.querySelector('.recent')
    let searched=document.querySelector('.searchButton')
    recently.addEventListener('click',function(e)
    {console.log("hahah")
     location.href="/recently"
    })
    searched.addEventListener('click',function(e)
    {
        location.href="/search"
    })

</script>
</body>
</html>